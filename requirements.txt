# --- Required ---------------------------------------------------------------
PyQt6>=6.5
PyYAML>=6.0          # config/options.yaml 읽기/쓰기

# PySide6 를 쓰고 싶다면 PyQt6 줄을 지우고 아래 줄의 주석을 해제하세요.
# 코드가 자동으로 PyQt6 -> PySide6 순서로 바인딩을 찾습니다. (dl_exp_manager/qt.py)
# PySide6>=6.5

# --- Optional ---------------------------------------------------------------
# options.yaml 을 손으로 편집하면서 주석을 유지하고 싶다면 설치하세요.
# 설치돼 있으면 앱이 자동으로 이 백엔드를 쓰고, 없으면 PyYAML 로 동작합니다
# (이 경우 UI 에서 설정을 바꿀 때 주석이 사라집니다).
# ruamel.yaml>=0.18

# 테스트
# pytest>=7.0
