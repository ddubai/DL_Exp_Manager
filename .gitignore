__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/
.idea/
.vscode/
.DS_Store

# 로컬 실험 DB / 내보내기 결과
experiments.db
experiments.db-journal
experiments.db-wal
experiments.db-shm
backups/
*.sqlite
exports/

# 설정 파일 백업/임시본 (config/options.yaml 자체는 추적한다)
config/*.yaml.bak
config/*.yaml.tmp

# 실서버 IP/구성이 들어가는 실제 servers.yaml 은 커밋하지 않는다.
# config/servers.template.yaml 을 복사해서 로컬에서만 정의한다.
config/servers.yaml

# uv 환경
.python-version
pyproject.toml
*.bak